<h1>All about <%= @kitten.name %> </h1>

<%# Toggle show & hide kitten "menu" %>
<div data-controller="toggle" data-toggle-hidden-class="hidden">
  <button data-action="click->toggle#toggle">Show/Hide Kitten</button>
  <div data-toggle-target="toggleable" class="hidden">
    🐱🐈
  </div>
</div>

<hr>

<div>
  <p>I'm a <%= @kitten.name %>.</p>
  <p>I'm <%= @kitten.age %> years old.</p>
  <p>I'm <%= @kitten.cuteness %> cute.</p>
  <p>My softness is <%= @kitten.softness %> out of 10.</p>
</div>

<hr> 

<%# Checkbox highlighting %>
<div data-controller="toggle" data-toggle-highlight-class="bg-yellow">
  <div data-toggle-container>
    <label>
      <input type="checkbox" data-action="change->toggle#toggleHighlight">
      I agree that <%= @kitten.name %> is so adorable!
    </label>
  </div>
</div>

<hr>

<%# Click to hide and show another element %>
<div data-controller="toggle" data-toggle-hidden-class="hidden">
  <div data-action="click->toggle#hide" class="cursor-pointer">
    Click me to see kitten actions! 👆
  </div>
  <div data-toggle-target="toggleable" class="hidden">
    <%= link_to 'Edit', edit_kitten_path(@kitten) %>
    <%= button_to 'Delete', kitten_path(@kitten), method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
</div>

<hr>

<%# Character counter %>
<div data-controller="character-counter" data-character-counter-max-value="280" data-character-counter-warning-value="50">
  <label>Send a message to <%= @kitten.name %>:</label>
  <textarea 
    data-character-counter-target="input"
    data-action="input->character-counter#updateCounter"
    rows="4"
    placeholder="Send a message to this kitty"></textarea>
  <div data-character-counter-target="counter"></div>
</div>
